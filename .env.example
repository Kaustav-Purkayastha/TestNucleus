# Paste your DB credentials here, then reference them in YAML configs as ${VAR_NAME}
# Example: connection: "postgresql://${PG_USER}:${PG_PASS}@${PG_HOST}/${PG_DB}"

# PG_USER=myuser
# PG_PASS=mypassword
# PG_HOST=localhost
# PG_DB=mydb

# MYSQL_URL=mysql+pymysql://user:password@localhost:3306/mydb
# SNOWFLAKE_URL=snowflake://user:password@account/db/schema?warehouse=wh
